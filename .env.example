DATABASE_URL=postgres://otel:otel@localhost:5432/otelforge?sslmode=disable
RABBITMQ_URL=amqp://otel:otel@localhost:5672/

# Required secrets — generate unique values before any non-local deployment.
# JWT_SECRET=$(openssl rand -hex 32)
# ENCRYPTION_KEY=$(openssl rand -hex 16)  # exactly 32 hex chars (16 bytes) for AES-256-GCM
JWT_SECRET=dev-jwt-secret-change-in-production
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef

API_ADDR=:8080
CORS_ORIGIN=http://localhost:5173
WORKER_CONCURRENCY=2
